<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.loveboyuan.smarttrader.SelectInventoryItemActivity">

    <ListView
        android:layout_width="fill_parent"
        android:layout_height="48dp"
        android:id="@+id/selectInventoryItemActionBar"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:layout_above="@+id/selectListView">
    </ListView>

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/selectListView"
        android:choiceMode="multipleChoice"
        android:layout_above="@+id/gobackButton" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="OK"
        android:id="@+id/gobackButton"
        android:textSize="24sp"
        android:textColor="#FFFFFF"
        android:background="#FF9E00"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_alignEnd="@+id/selectListView"/>

</RelativeLayout>
